export function Footer() {
  return (
    <footer className="wrap">
      <span>Pulse · teaching preview · tokens locked gates 1–6</span>
      <span>hairline + elev-1 · accent only on signal</span>
    </footer>
  );
}
